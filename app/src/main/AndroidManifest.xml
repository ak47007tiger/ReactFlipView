<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.doocom.pagelistview"
  >

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:supportsRtl="true"
    android:theme="@style/AppTheme"
    >
    <activity android:name=".PageListBasicDemo">
    </activity>
    <activity android:name=".TestMeshImageActivity">
    </activity>
    <activity android:name=".usefoldview.UseFoldViewActivity">
    </activity>
    <activity android:name=".useflipview.UseFlipViewActivity">

    </activity>
    <activity android:name=".useflipview.UseReactFlipViewActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>
  </application>

</manifest>